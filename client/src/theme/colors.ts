import { ThemeType } from './types'

const colors: ThemeType['colors'] = {
  bg: '#f2f2f2',
  text: 'black',
  primary: 'black',
  secondary: 'orange',
  borderSecondary: 'yellow'
}

export default colors
